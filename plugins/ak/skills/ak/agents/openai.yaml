interface:
  display_name: "Access Modernization Kit"
  short_description: "Run six-phase, multi-agent legacy investigations"
  default_prompt: "Use $ak V2.1 to coordinate an evidence-backed six-phase investigation for one legacy SMS app. Extract Access binaries from immutable snapshots, normalize build context without executing commands, process deterministic modules leaf-first, keep app data isolated, publish phases sequentially, and require independent QA before rendering derived outputs."
